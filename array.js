const ps= require("prompt-sync");
const prompt=ps();
var inputArray = [];
n=prompt("Enter the length ");
console.log("Enter the Elements");
for(var i=0; i<n; i++) 
{
	inputArray[i] = prompt( );
}
console.log(inputArray);
